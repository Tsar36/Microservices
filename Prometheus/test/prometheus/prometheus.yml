global:
scrape_interval: 20s
evaluation_interval: 20s

# Add mark for graphical presents
exernal_labels: monitor: 'Docker-my-monitor'

# End points for collect info
- job_name: 'my_prometheus'
scrape_interval: 25s
static_configs:
- targets: ['localhost:9090']
